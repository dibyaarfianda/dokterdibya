# Task 6 independent review — `e6105ef6..81682db9`

**SPEC COMPLIANCE: FAIL. CODE QUALITY: FAIL.** Review of implementation `81d219cc` and report `81682db9`; no implementation edits, production access, push, or deployment. The failures below concern the complete Task 6 contract, not the acknowledged pre-deployment RUM/release gate.

## Findings (highest severity first)

1. **P1 — Final Staff UI is not last-navigation-wins outside the new wrapper.** `staff/public/scripts/main.js:628-637,1807-1823,1827-1833` awaits `staffPageRegistry.activate()` or `ensureRegisteredPage()` and then commits `hideAllPages()`, page visibility, title, and active nav without checking whether the registry returned `null` for a superseded activation. Example: a slow `showPatientPage()` starts; `showDashboardPage()` completes and shows Dashboard; the old patient activation then returns `null`, yet `showPatientPage()` shows Data Pasien. The `registeredPageNavigationGeneration` check at lines 552-570 protects only `activateRegisteredStaffPage`, not these real menu callers or synchronous page transitions. **Impact:** a late navigation can replace the user's most recent view and focus/context. **Fix:** route every final UI commit through one shared navigation generation (including direct and synchronous handlers), and stop when the registry reports supersession; guard post-commit asynchronous dashboard effects too. **RED test:** drive the actual `main.js` menu handlers with deferred patient/dashboard loads, resolve them out of order, and assert only the latest page, title, nav, and `__currentPage` remain.

2. **P1 — The production performance workflow cannot reach its budget checks on a clean runner.** `.github/workflows/staff-performance-budget.yml:17-25` checks out code, sets up Node, then immediately runs the script; it never installs `staff/backend` dependencies. `staff/backend/scripts/perf-budget-check.js:12` requires `puppeteer` at module load, which is declared in `staff/backend/package.json` but not checked into Git. A fresh GitHub runner therefore exits with module resolution failure before the authenticated Staff URL, cold/warm, or cached-activation checks run. **Impact:** the required scheduled/manual guardrail is permanently red for infrastructure reasons and supplies no performance evidence. **Fix:** run `npm ci` in `staff/backend` (and ensure a matching Chromium executable is available) before invoking the script; retain the secret only in the environment. **RED test:** execute the workflow steps in a fresh checkout/container without preinstalled `node_modules` and require it to reach a synthetic budget assertion, while separately proving missing secret fails closed.

3. **P2 — Patient service-worker precache keys do not match many resources actually requested by the standalone PWA.** `public/sw.js:92-95` rewrites every static URL to `?v=20260924wave3`, whereas `public/patient-menu.html:15,2644` requests an unversioned icon and `patient-portal-theme.css?v=20260528portal1`; `public/scripts/patient-menu-shell.js:1` imports another script with `?v=20260924realtime2`. On network failure, `public/sw.js:171-185` performs exact `cache.match(request)` and returns no cached response for these URLs. **Impact:** the newly installed version appears atomic but its precached icons/styles/scripts are unavailable through normal offline/failing-network requests, regressing standalone PWA static fallback. The approved HTML/API no-store rule does not require this static mismatch. **Fix:** derive precache request keys from the URLs the shell actually uses (or normalize lookup safely to a versioned canonical asset), without caching HTML/API or third-party assets. **RED test:** install the worker in a Cache Storage fixture, request representative actual icon/CSS/module URLs with network rejected, and assert a current-version cached response.

4. **P2 — RUM can still retain a patient identifier in the first API path segment.** `staff/backend/routes/rum.js:68-75` replaces segments after `/api/<segment>` but permits any alphanumeric first segment. A synthetic probe of the actual function returned `/api/SyntheticPatientName/:path` for `/api/SyntheticPatientName/visits`. The unauthenticated POST at lines 231-235 records that bucket and the summary exposes its key. **Impact:** the Task 6 promise that RUM is identifier-free is not enforced at ingestion for malformed or unexpected client paths. **Fix:** allowlist known static API route families on the server and map all unknown first segments to `/other`; keep client bucketing but do not trust it. **RED test:** POST a synthetic alphabetic sentinel in the first API segment, fetch the authorized summary, and assert the sentinel is absent while known route categories remain visible.

5. **P2 — The required masked visual snapshot gate is not implemented.** `staff/backend/tests/unit/Task6StructuralSnapshot.test.js:12-23,26-45` hashes HTML tags and compares the *same current markup* at two viewports with JavaScript disabled. It never captures screenshots, masks dynamic rendered regions, or compares desktop/PWA visuals to approved baseline images. CSS or layout regressions can pass unchanged tag hashes and same-DOM assertions. The implementation report itself labels this structural-only evidence. **Impact:** Task 6's explicit visual/no-structural-difference release criterion has no effective visual regression check. **Fix:** add baseline browser screenshots for the real desktop and standalone PWA shells at fixed viewports, mask dynamic regions, and compare rendered output; keep the current DOM hash as a supplementary guard. **RED test:** introduce a synthetic CSS/layout displacement with unchanged DOM and prove the visual gate fails.

## Verified boundaries and test evidence

- Storage invalidation is limited to `staff_asset_manifest` and `user_roles_` role-cache keys; auth, drafts, filters, and context are not cleared. Profile/intake precedes independent settings/count reads; nickname and birth-state ordering is preserved. Polling implementations avoid overlapping hidden reads and queue a visible refresh. Staff/patient cache deletion is app-prefixed; failed precache rejects install; current Staff HTML/worker references use `v413`.
- The performance script reads `STAFF_PERF_TOKEN` from the environment, not argv, targets the exact Staff route, and numerically checks warm requests/failures and cached activation. It was **not** run against production. Lowercase vitals and cached activation aggregate canonically. The RUM bucket exception above remains.
- Focused tests: 6 suites/32 tests passed (`Task6ServiceWorker`, `Task6PerformanceGate`, `Task6Polling`, `Task6PatientBootstrap`, `StaffPanelWave2LazyShell`, `StaffPanelWave3Lifecycle`). `observabilityAuth` passed. `Task6StructuralSnapshot` passed 4/4 after pointing Puppeteer to installed local Chrome; an initial run without that local browser failed for environment setup only. These passing tests do not cover the findings. No broad suite was rerun.
- Review report remains uncommitted because findings require implementation changes and RED coverage. No push/deploy/production access was performed.

## Round 1 re-review — `81682db9..ead639ed`

**SPEC COMPLIANCE: FAIL. CODE QUALITY: FAIL.** The targeted corrections for the original five findings are present and focused tests pass, but the complete Task 6 navigation/telemetry/visual-CI contract still has the following gaps. This is a read-only implementation re-review; no report commit because findings remain.

### Remaining findings

1. **P2 — A visually superseded registry activation still emits stale page state and restarts wrong-page polling.** `staff/public/scripts/main.js:415-426,500-532,1984` increments only its own `staffNavigationGeneration` when a synchronous page (for example Finance) wins. The in-flight `PageRegistry.activate('patients')` is not invalidated: `staff/public/scripts/shell/page-registry.js:87-105` then sets `activeKey='patients'` and emits `page:changed`, which `staff/public/scripts/shell/polling-coordinator.js:15-22` consumes. The final main.js UI guard prevents the old Patients *view* from replacing Finance, but not this later coordinator state change. A synthetic probe confirmed final UI `finance`, registry active `patients`, and a late `page:changed`. **Impact:** page-scoped patient/dashboard polling can run for a page no longer shown; the active registry key disagrees with UI and can affect later deactivation. **Fix:** make all navigation paths invalidate the registry's pending activation and publish one authoritative page event/state, or pass the shared generation into `PageRegistry.activate` and suppress stale state/event commits. **New RED test:** start a deferred Patients registry activation, click synchronous Finance, resolve Patients, then assert Finance remains visible **and** registry/coordinator active page is Finance (or none), with no late Patients event/job.

2. **P2 — Identifier-free RUM is still not enforced for page and error fields.** The first API segment is now allowlisted in both client and server, resolving the reported path case. But `staff/backend/routes/rum.js:92-111,218-220,249-253` still stores client-supplied `page` (letters, digits, slash) and free-form `errors[].message`; the matching client scrubber at `staff/public/scripts/rum.js:146-174,196` does not remove ordinary names. A synthetic invocation of the actual server sanitizer returned `Failed to save patient Synthetic Name in chart` unchanged. **Impact:** a runtime error or crafted RUM beacon can put a patient name in in-memory telemetry and the superadmin summary, contrary to Task 6's no-patient-identifier requirement. **Fix:** allowlist canonical page keys at ingestion and stop retaining raw error messages; store a bounded error category/opaque fingerprint only, while keeping timings and status. **New RED test:** submit a synthetic name in `page` and `errors[].message`, read the authorized summary, and assert no sentinel survives while the intended metric aggregates do.

3. **P2 — Patient PWA visual baseline is not available in CI for patient-only changes.** `.github/workflows/staff-panel-ci.yml:4-12,35` now fetches full history and runs all unit tests (including the new pinned visual test), but its push and PR path filters include only `staff/**` and the workflow file. A commit touching only `public/patient-menu.html`, `public/sw.js`, or patient CSS will not start this workflow. **Impact:** the explicit PWA visual regression gate is silently absent for the changes it is intended to guard. **Fix:** include `public/**` (and any relevant visual baseline inputs) in both workflow path filters, or put the visual test in a separately triggered patient workflow. **New RED test/check:** evaluate the workflow trigger against a synthetic patient-only diff and require the visual job to run.

### Re-verification

- The original direct final-UI race, clean-runner performance bootstrap/secret order, owned-path SW offline fallback, first-segment API bucket, and pinned screenshot/negative-control assertions were inspected in the changed source and tests. The focused navigation/performance/SW/RUM/visual set passed **6 suites, 35 tests**. No production performance run was attempted. Actual clean Ubuntu `npm ci`/Chromium setup remains CI evidence, not a local claim.
- The visual test renders the pinned `e6105ef6` source/assets through Chromium and checks an injected CSS displacement; `fetch-depth: 0` makes the commit available when Staff CI runs. The SW's `ignoreSearch` lookup is restricted to listed same-origin static paths; HTML/API remain network-only, with app-scoped cache ownership. Patient `20260924wave3r1` registration/cache/manifest sources match, and the new precache files exist.
- A fresh all-unit run returned exactly the reported baseline counts: **12 suites/13 tests failed; 157 suites/1167 tests passed** (169 suites/1180 tests total). Jest retained existing open handles after its summary, so the process was interrupted after those counts; no new test-count regression was observed. Focused visual tests ran with installed local Chrome. No implementation edit, push, deployment, or production access was performed.

## Round 2 re-review — `ead639ed..8f1a3511`

**SPEC COMPLIANCE: PASS. CODE QUALITY: PASS** for the reviewed Task 6 implementation and this scoped fix, subject to the already documented controller/CI/deployment/live performance release gates. No new actionable findings. The original five findings and the three Round 1 findings are resolved in the inspected code and focused behavior tests; this verdict does not claim production success.

- **Navigation:** `reserveStaffNavigation()` invalidates a pending registry activation, and external menu commits update the registry and emit one authoritative `page:changed`. The actual-handler Finance-versus-slow-Patients test now asserts visible DOM, title, `registry.activeKey`, final event list, and `PollingCoordinator.activePage`/job eligibility all stay on Finance. A separate synthetic probe confirmed invalidation returns the stale activation as `null` without clearing the shared fragment-load promise: later Patients activation reused one fetch and committed normally. The existing PageRegistry load/lifecycle and last-navigation tests passed.
- **RUM privacy:** Browser and server independently allowlist page/API keys. Browser role is the fixed `staff` category; errors contain only an allowlisted type, generic message, and type-derived fingerprint. Server ignores client-provided error prose/fingerprint/role and retains only coarse type, generic message, counts, and timestamps. The synthetic browser beacon and authenticated server summary tests reject names, MR numbers, email, free prose, and arbitrary page/error identifiers while preserving known dashboard/other LCP and distinct known API buckets.
- **CI and visual/SW gates:** Staff CI push and PR filters now include both `staff/**` and `public/**`; synthetic patient-only paths match, docs-only does not. Its existing all-unit command includes the pinned `e6105ef6` Chromium visual/negative-control and service-worker suites; checkout uses full history. The performance workflow still checks for a secret before locked `npm ci`, installs Puppeteer Chrome, and invokes the exact Staff URL without argv credentials. Actual clean Ubuntu CI and live budget evidence remain release gates, not local claims.
- **Other Task 6 boundaries:** the reviewed storage registry, profile/intake/nickname/birth bootstrap order, in-flight/hidden polling, app-scoped atomic SW caches and no-store HTML/API, version sources, warm/activation numeric budgets, and structural/visual snapshot behavior were rechecked through unchanged focused contracts and the full unit sweep. No product-code modification was made during review.

Fresh verification: the focused navigation/RUM/workflow/SW/visual/observability set passed **8 suites, 48 tests**; the full unit sweep reported **159 suites/1171 tests passed and 12 suites/13 tests failed** (171/1184 total). A filtered repeat confirmed the same 12 failing suite names listed in the Round 2 implementation report, not merely the same count; no new unit failure was observed. `git diff --check`, syntax checks for the changed production JavaScript, and the Staff static check passed (cache `v413`). No push, deployment, production access, or patient-data operation occurred in this review.
