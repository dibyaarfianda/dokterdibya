# Task 6 implementation report

## Scope and result

Implementation commit: `81d219cc4c21f308aed2e18a2540b759e1e427e2` on `codex/system-hardening-seamless`, starting from `e6105ef65dcd5585ef6ba271733ca887b1292d12`. This report is committed separately so it can name the implementation SHA. No push, deployment, production access, real patient operation, dependency update, or Wave 4 work was performed, as explicitly directed for this SDD subphase.

The implementation follows Task 6's approved nonvisual direction:

- Staff asset-version cutover now removes only registered disposable local cache keys, without clearing either storage area. Authentication, clinical drafts, filters, selected patient, and last navigation remain untouched.
- `PageRegistry` has a last-navigation generation guard while `ensureLoaded` still coalesces a page's fragment load. The final `main.js` UI commit independently checks its navigation generation. Cached menu activation duration is measured with no patient/MR argument.
- Patient profile remains the first authenticated/intake gate. An incomplete intake now stops bootstrap. After that gate, portal settings and the independent notification-count read overlap; nickname still waits for settings, and birth pending/congratulations retain their original order. `loadProfile()` sets `PatientSession` identity, `fetchPortalSettings()` consumes and rechecks that identity, and the nickname gate consumes `portalSettings.nickname`: those three operations cannot safely start concurrently despite the high-level performance aspiration.
- Coordinator jobs, staff notification badges/count, and patient home queue pause when hidden, avoid overlapping reads, and request one immediate refresh on return to visible; an in-flight read completes before the queued refresh. Normal intervals remain unchanged.
- Staff/patient workers use app-scoped versioned static caches, reject failed precache installs, keep HTML/API network-only/no-store, and delete only their own old caches. Patient worker `SKIP_WAITING` checks use logical conjunction. Staff `v413` and patient `20260924wave3` references are synchronized across the shell, worker, compatibility worker, landing, and manifests. No markup/CSS layout redesign was made.
- Performance workflow targets `/staff/public/index-adminlte.html`, accepts the staff token only through `STAFF_PERF_TOKEN`, and fails when absent. It measures cold and warm loads and five cached dashboard activations, enforcing warm requests <=40, zero failed warm requests, and cached activation p95 <=1000 ms. RUM accepts existing uppercase and live lowercase vitals, aggregates them canonically, allowlists cached activation, and buckets API paths without query or dynamic patient path segments.

## TDD evidence

Local Jest commands used `NODE_PATH=D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules` because dependencies are installed in the saved checkout rather than this worktree; Chromium structural tests additionally used `PUPPETEER_EXECUTABLE_PATH=C:/Program Files/Google/Chrome/Application/chrome.exe`.

Each family was run against the pre-patch implementation before editing production code:

| RED focused command suffix (`jest --runInBand --forceExit --coverage=false`) | Baseline failure |
| --- | --- |
| `tests/unit/RealtimeClientAuth.test.js -t 'asset cache cutover'` | 1 failed: clinical draft and selected patient erased |
| `tests/unit/StaffPanelWave2LazyShell.test.js -t 'last navigation'` | 1 failed: late page became active |
| `tests/unit/StaffPanelWave3Lifecycle.test.js -t 'resumes once'` | 1 failed: no immediate resume |
| `tests/unit/StaffPanelWave3Lifecycle.test.js -t 'performance workflow'` | 1 failed: skip-on-missing secret, CLI token, wrong URL |
| `tests/integration/observabilityAuth.test.js -t 'live lowercase'` | 1 failed: frontend vital payload returned HTTP 400 |
| `tests/integration/observabilityAuth.test.js -t 'path buckets'` | 1 failed: patient path sentinel appeared in RUM summary |
| `tests/unit/Task6ServiceWorker.test.js` | 5 failed: partial installs resolved, unrelated caches deleted, bitwise message condition |
| `tests/unit/Task6ServiceWorker.test.js -t 'patient precache'` | 1 failed: unversioned/third-party static assets |
| `tests/unit/Task6ServiceWorker.test.js -t 'both workers treat'` | 1 failed: non-navigation patient HTML lacked no-store branch |
| `tests/unit/Task6Polling.test.js` | 2 failed: missing visible badge refresh and hidden patient queue poll |
| `tests/unit/Task6Polling.test.js -t 'notification count'` | 1 failed: in-flight visible refresh was dropped |
| `tests/unit/Task6PatientBootstrap.test.js` | 2 failed: incomplete intake did not return false; no independent overlap |
| `tests/unit/Task6PerformanceGate.test.js` | RED: importing the gate called `process.exit(1)`; missing numeric fixture was later shown to pass incorrectly |

GREEN final focused command: `node D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules/jest/bin/jest.js --runInBand --forceExit --coverage=false --silent tests/unit/RealtimeClientAuth.test.js tests/unit/StaffPanelWave1Hardening.test.js tests/unit/StaffPanelWave2LazyShell.test.js tests/unit/StaffPanelWave3Lifecycle.test.js tests/unit/Task6ServiceWorker.test.js tests/unit/Task6Polling.test.js tests/unit/Task6PatientBootstrap.test.js tests/unit/Task6StructuralSnapshot.test.js tests/unit/Task6PerformanceGate.test.js tests/unit/WebHardeningPlan.test.js tests/integration/observabilityAuth.test.js` — **11 suites, 83 tests passed**.

The deferred-promise bootstrap test demonstrates the critical path: settings and notification count do not start before profile/intake; they start together after the profile promise resolves; nickname does not run until both resolve; content remains hidden until nickname succeeds; birth pending precedes birth congratulations.

The structural tests mask dynamic script/style bodies and cache-version query values and compare approved tag/attribute hashes. A Chromium DOM test then compares the same masked element hierarchy at 1366×768 desktop and 390×844 mobile/PWA viewports with scripts disabled. This is structural equivalence evidence, **not** a production screenshot or pixel-level visual assertion.

## Final validation

- Full integration: `jest --runInBand --forceExit --coverage=false --silent --testPathPatterns=integration` — **23 suites, 238 tests passed**.
- Existing staff smoke: four prescribed suites (`StaffPanelWave1Hardening`, `StaffPanelWave2LazyShell`, `StaffApiClient`, `observabilityAuth`) — **4 suites, 34 tests passed**.
- `node scripts/staff-static-check.js` — passed; HTML 317544 bytes, inline JS 41162 characters, cache `v413`.
- `node --check` — passed for 21 changed/new JavaScript files. `git diff --check` and staged diff check — passed. Package has no applicable frontend build script; no dependency/build configuration was changed.
- CLI failure proof: with no `STAFF_PERF_TOKEN`, `node scripts/perf-budget-check.js --base-url http://127.0.0.1:9` exited 1 with only a configuration message; supplying a synthetic `--token` was also rejected without printing its value. Browser performance against a real authenticated target was deliberately not run locally.

Controller-supplied read-only pre-Task6 production baseline (not local verification): `/api/rum/summary` HTTP 200 with empty vitals/API; five warm reload load times `[523,433,501,385,300]` ms, p75 `501` ms and p95 `523` ms, resource counts `[83,75,59,84,59]`; one cache-disabled cold load DCL `341` ms, load `460` ms, 75 resources, 710255 transferred bytes. Post-deployment controller gates remain p75 <=375.75 ms (25% improvement) and p95 <=549.15 ms (<=5% regression), alongside CI's <=40/zero-failure/cached <=1000 ms fixture. No post-deployment improvement is claimed.

## Self-review and remaining gates

The diff contains only Task 6 shell, worker, RUM/performance, and focused test files. No patient identifier is supplied to the new cached-activation metric; RUM API query strings and post-resource path segments are discarded on both client and server. Potential tradeoff: a poll that never settles will defer visible refresh indefinitely rather than overlap requests; normal API requests have their existing timeouts. The patient worker no longer serves a cached HTML offline page because the approved no-store HTML rule takes precedence; static assets remain available from the versioned cache. These behavior boundaries should be reviewed before rollout.

Release still requires controller review, CI with a configured `STAFF_PERF_TOKEN`, production deployment via the approved atomic process, live asset/cache and desktop/PWA verification, a staffed-session RUM comparison to the controller baseline, and rollback if the p75/p95 gate fails. This task was intentionally not deployed.

## Review Round 1 correction

The five findings in `task-6-review.md` were addressed on top of `81682db9`, with no push, deploy, production access, patient data, or dependency-version change. This correction follows the approved nonvisual Task 6 direction. The prior structural-only visual claim above is superseded by the screenshot comparison below.

### RED evidence and correction

All commands below used `NODE_PATH=D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules` and `node D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules/jest/bin/jest.js --runInBand --forceExit --coverage=false` from `staff/backend`; Chromium tests also used `PUPPETEER_EXECUTABLE_PATH=C:/Program Files/Google/Chrome/Application/chrome.exe`.

| Finding | Focused RED against prior implementation | Correction and GREEN |
| --- | --- | --- |
| Final Staff UI race | `tests/unit/Task6NavigationRace.test.js`: four direct-handler/synchronous races failed; `-t registered-fragment`: six actual async handlers failed after a later Finance click. | One shared `staffNavigationGeneration` now governs registry, legacy direct, sync, and delayed commits, including Sunday, cashier, hospital, and Kantor callbacks. `Task6NavigationRace`: **10/10 passed**. |
| CI dependency bootstrap | `tests/unit/Task6PerformanceGate.test.js`: workflow test failed because `npm ci` was absent. | Credential precheck, locked `npm ci`, and Puppeteer Chrome installation precede the gate. Secret exists only in step environment, not argv. Missing-secret subprocess exits 1 before a request. **3/3 passed**. |
| Patient SW offline keys | `tests/unit/Task6ServiceWorker.test.js -t shell-owned`: actual shell asset URL failed offline; static import graph then exposed two uncached community modules. A cache-namespace test separately failed while still at `20260924wave3`. | Added four missing shell/import scripts; versioned precache lookup ignores query **only** for same-origin app-owned immutable paths. Rotated the patient worker, compatibility registration, shell, landing, and manifest versions together to `20260924wave3r1` so a failed install cannot delete the active cache. HTML/API remain no-store; foreign/staff assets cannot borrow cache entries. **11/11 passed**. |
| RUM first path segment | `tests/integration/observabilityAuth.test.js -t unknown first API` exposed synthetic first-segment sentinel; `tests/unit/Task6RumPathPrivacy.test.js` exposed it on the client. | Client and server now emit only allowlisted static route-family buckets; unknown families collapse to `/other`. Distinct known categories remain separate. Combined **8/8 passed**. |
| Visual regression | `tests/unit/Task6VisualBaseline.test.js` initially failed because the screenshot comparator was absent. | Chromium now renders approved commit `e6105ef6` and current Staff desktop (1366×768) and patient PWA (390×844) shells with real local CSS/assets, JavaScript disabled, external requests blocked, deterministic dynamic-region/animation masking, and pixel comparison. An injected 80px CSS displacement with identical DOM exceeds the failure threshold. **3/3 passed**. Staff CI fetches full Git history so the approved baseline commit is available. |

The masked visual gate is a local/CI structural pixel comparison, not live-browser or post-deployment evidence. It intentionally ignores sub-24-channel pixel noise and allows <0.2% differing pixels, while the synthetic displacement must exceed 2%. It fetches approved source/assets from the pinned commit at test time, rendering both sides with the same Chromium binary; this avoids comparing a Windows screenshot artifact against a Linux CI browser. It does not execute clinical scripts or load patient data.

### Validation and limits

- Focused Task 6 round: 11 suites, **63 tests passed** after the import-graph and cache-rotation additions (navigation, workflow, SW, RUM, visual, existing structural/bootstrap/polling/lifecycle, and RUM integration).
- Full integration: 23 suites, **239 tests passed**.
- Existing Staff smoke: 4 suites, **35 tests passed**.
- Staff static check passed (HTML 317544 bytes; inline JS 41162 chars; cache `v413`); `node --check` passed for changed JavaScript; `git diff --check` passed. The Stage 2 main.js byte-budget assertion remains below 218000 bytes after shortening local variable names without changing behavior.
- The broader all-unit sweep is **not green** but returned to the controller's `fc0a483f` baseline failure counts: **157 suites/1167 tests passed; 12 suites/13 tests failed** (169 suites/1180 tests total). The unchanged failures include native Gambiran media conversion and older static modularity/visual assertions. The one newly crossed main.js size threshold was corrected; an additional Task 6 patient-shell size threshold was corrected by tightening only its new queue/bootstrap code to 171977 normalized bytes (<172000), with deferred bootstrap and polling behavior tests still green. Neither failure remains in the final sweep.
- A separate clean-copy `npm ci` rehearsal could not start locally because this Codex Windows runtime has Node but no `npm` executable or npm CLI (`CommandNotFoundException`). The workflow order/lockfile resolution and missing-secret failure are tested, but actual clean Ubuntu dependency/browser installation remains a CI release gate; no local success is claimed.

Self-review: the Staff generation guard is shared by final UI commits, preserving same-page PageRegistry coalescing. SW ignore-search never applies to HTML/API, other origins, or Staff paths. RUM buckets cannot hold arbitrary first segments, though less common API families intentionally aggregate as `/other`. Visual source is pinned to the pre-change commit and cannot silently compare two copies of current markup. No visible UI/layout/CSS was changed. Review, CI, staged dry-run deployment, live performance comparison, and the approved release gates remain pending.

## Review Round 2 correction

Starting from `ead639ed220881c3b95cddb873e9ba8644433115`, the three remaining P2 findings in `task-6-review.md` were corrected sequentially without subagents, dependency changes, push, deploy, production access, or patient data. This remains within the approved nonvisual Task 6 plan.

### RED to GREEN

Commands used `NODE_PATH=D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules` and `node D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules/jest/bin/jest.js --runInBand --coverage=false` in the worktree. The browser-dependent sweep also set `PUPPETEER_EXECUTABLE_PATH=C:/Program Files/Google/Chrome/Application/chrome.exe`.

| Finding | Valid RED before implementation | Correction |
| --- | --- | --- |
| Split navigation state | `staff/backend/tests/unit/Task6NavigationRace.test.js`: the new actual-handler Finance/slow-Patients test failed because the visible Finance page coexisted with stale `registry.activeKey='patients'` and a late Patients event. | A direct navigation invalidates the registry generation before committing. Synchronous/direct pages publish through `PageRegistry.navigateExternal`; registry, `page:changed`, `__currentPage`, title, visible DOM, and PollingCoordinator now agree. Existing same-page fragment-load coalescing remains covered by `StaffPanelWave2LazyShell`. |
| RUM page/error privacy | `staff/backend/tests/integration/observabilityAuth.test.js`: synthetic patient name in `page`, client fingerprint, and free clinical prose survived into summary. `staff/backend/tests/unit/Task6RumPayloadPrivacy.test.js`: after correcting the VM's missing `fetch` fixture, the actual beacon failed because `page` contained the synthetic name. A subsequent role-field probe failed because the browser still sent a synthetic name from `__userRole`. | Both browser and server map pages to fixed code-owned keys, unknown to `other`. Browser sends only a coarse error type/generic message/type-derived hash and a fixed `staff` role category; server discards client fingerprint and prose and owns type/fingerprint/count. Known dashboard LCP and unknown-page `other` LCP remain separately aggregated. Old static test was updated to assert the new coarse contract; behavioral tests assert name/MR/email/number/prose do not survive. |
| Patient-only CI gap | `staff/backend/tests/unit/Task6WorkflowPaths.test.js`: both push and PR synthetic patient-only paths failed the workflow trigger check. | `staff-panel-ci.yml` includes `public/**` in both event filters, while unrelated `docs/**` remains excluded. The same job's existing all-unit command includes pinned Chromium visual and service-worker tests. |

The GREEN focused command for the new regressions (`Task6WorkflowPaths`, `Task6NavigationRace`, `Task6RumPayloadPrivacy`, and `observabilityAuth`) passed **4 suites / 22 tests**. The extended shell/polling/RUM focused command (`Task6NavigationRace`, `Task6RumPayloadPrivacy`, `Task6RumPathPrivacy`, `Task6WorkflowPaths`, `Task6Polling`, `StaffPanelWave1Hardening`, `StaffPanelWave2LazyShell`, `StaffPanelWave3Lifecycle`, `observabilityAuth`) passed after the static-contract update; the initial run exposed only that old test's literal requirement for the removed sanitizer function, and the new integration test already proved the replacement behavior.

### Validation and self-review

- Full integration: `jest --runInBand --coverage=false --silent --forceExit --testPathPatterns=integration` — **23 suites / 240 tests passed**, exit 0.
- Existing Staff smoke (the four prescribed suites with `--forceExit`) — **4 suites / 36 tests passed**, exit 0.
- Whole unit sweep with local Chrome and `--forceExit` — **159 suites / 1171 tests passed; 12 suites / 13 tests failed** (171 suites / 1184 tests). The failure count matches the recorded pre-round baseline; the current failing suites are `GambiranResumeArtifacts`, `SundayClinicClosingPaymentGuard`, `SundayClinicPwaVisualRefactor`, `StaffPanelWave6StartupSchema`, `FrontendModularizationStage2Wave2/3/4`, `SundayClinicWave5Modularity`, `PatientPortalHeaderBranding`, `RegistrationCodeHeader`, `docboardGambiranResumeAccess`, and `docboardAuraScheduleAccess`. Two new suites/four tests account for the total increase from 169/1180; changed-area focused gates pass. The first sweep without `PUPPETEER_EXECUTABLE_PATH` additionally failed the two browser suites for missing Chrome, so it is **not** the comparison run.
- `node staff/backend/scripts/staff-static-check.js` passed (HTML 317544 bytes; inline JS 41162 characters; cache `v413`). `node --check` passed on changed production/test JS. `git diff --check` passed; Windows line-ending warnings were informational.

Self-review found no layout/markup/CSS modification or clinical workflow mutation. Both RUM sides now reject arbitrary page and error text as telemetry categories; fixed known page categories remain useful, but an unlisted future page intentionally aggregates under `other` until explicitly reviewed. Direct navigation cancels only pending registry activation, not the existing coalesced load promise. The patient CI filter is confined to `public/**`, not all-repository changes. The pinned Chromium visuals ran as part of the whole-unit sweep; this is local structural evidence, not deployed behavior. Clean Ubuntu CI dependency/Chromium installation, controller review, and the approved production performance/live gates remain pending.

## Review Round 3 performance-auth hotfix

Starting from `8f1a351117d4aeb8689b5c2f6513f7f9acc79340`, the performance script's literal Staff storage key and persistent `localStorage.setItem` were removed. The change is limited to `staff/backend/scripts/perf-budget-check.js`, its focused test, and this report. No push, deploy, production request, CI token, or real patient data was used.

The reviewed Staff shell defines `window.TOKEN_KEY` in its first inline head script; its `getAuthToken()` later reads local then session storage using that key. The script now installs a `Page.evaluateOnNewDocument` read hook before navigation. Only the target-origin top-level document can receive the ephemeral environment-provided token, and only when the requested key equals the page-defined `window.TOKEN_KEY`; all other reads delegate to the original `Storage.prototype.getItem`. No storage write, literal key, CLI argument, or token log was introduced.

TDD RED: `tests/unit/Task6PerformanceGate.test.js` first failed the source contract on literal `vps_auth_token`/`localStorage.setItem`. The independent real-Chromium test then loaded a loopback synthetic Staff page defining a different `TOKEN_KEY` and failed because cold/warm auth probes returned 401 (warm failed requests 2). GREEN: the same page now produced two authenticated probes, with the pre-definition read and unrelated key still empty and both local/session storage lengths zero across cold and warm loads. The test deliberately asserts auth probes rather than unrelated request-failure counts, since a reload can count a canceled request in the warm phase.

Final validation with local Chrome (`PUPPETEER_EXECUTABLE_PATH=C:/Program Files/Google/Chrome/Application/chrome.exe`) and the saved checkout's `NODE_PATH`: `jest --runInBand --coverage=false --forceExit tests/unit/Task6PerformanceGate.test.js` — **1 suite / 5 tests passed**; `jest --runInBand --coverage=false --forceExit --silent --testPathPatterns=Task6` — **10 suites / 44 tests passed**; full integration — **23 suites / 240 tests passed**. Whole-unit sweep — **159 suites / 1173 tests passed, 12 suites / 13 tests failed** (171 suites / 1186 tests total), the same known failing-suite/count baseline as Round 2 plus the two new passing tests. `node --check` passed for both changed JavaScript files and `git diff --check` passed. Local Chromium is only synthetic validation, not a CI or production performance result. Clean Ubuntu CI and live release gates remain pending.

## Wave 3 performance-gate repair (local, nondeployment)

Starting from `92c773cd`, the performance gate was repaired after read-only diagnosis of the deployed pre-repair behavior. No production request, clinical write, push, or deployment occurred during this implementation. The approved nonvisual scope was preserved: no patient/staff layout or CSS redesign, and no patient page script changed.

The prior gate counted every Puppeteer request event (including memory/disk/SW cache hits), summed decoded bodies returned by `response.buffer()` even for 304/cache, and assigned an aborted cold long-poll to the mutable `warm` phase. It also scored API WAN round-trip time against application budgets. The corrected gate uses the installed Puppeteer 24 CDP session (`Network.requestWillBeSent`, `requestServedFromCache`, `responseReceived`, `loadingFinished`, `loadingFailed`) to bind each request to its start phase, count real network-backed transfers and pending network requests, and separate cache/SW hits. Conditional network 304s still count; warm-started 4xx/5xx and disconnects still fail. Its first-party JS/largest-image byte budgets now use encoded network transfer rather than decoded cached bodies. `Server-Timing: app;dur=<milliseconds>` is emitted before headers by the backend middleware and contains only a duration. The four API p95 budgets score that application duration; WAN wall p95 is printed only as diagnostic. Missing/malformed timing fails closed. The credential remains ephemeral, page-defined-key-only, and is never stored or logged.

The Staff shell no longer has the parser-blocking head `socket-credentials.js` tag: `main.js` statically imports `realtime-sync.js`, which imports the credential module before realtime initialization. The 900 ms patientTools/DataTables eager warmup was removed while menu shims remain lazy. Chat avatar `<img>` now has native lazy loading, async decode and 36×36 intrinsic dimensions; a real-Chromium loopback test observed zero closed-chat photo requests and one request after opening, with the same visible dimensions. To keep genuinely network-backed warm requests bounded, the Staff worker atomically precaches 32 verified existing, versioned, first-party initial-shell scripts in addition to its prior static assets; HTML/API remain network/no-store. Shell and worker versions rotate together to `v414`. Exact current-version requests and canonical imports from a client whose version was proved by an earlier explicit script may use the cache. A future-version document controlled by the old worker, or an unproven client, bypasses the old cache for all canonical imports. The bounded in-worker client-version map stores only opaque browser client IDs and versions, with no patient data or logging. The test's real Chrome SW fixture included a versioned bootstrap importing a canonical dependency and observed both served from current-version SW cache with <=2 network-backed warm requests.

TDD RED before production edits: `Task6PerformanceGate.test.js` plus `StaffStartupPerformance.test.js` — **2 suites failed, 9 tests failed / 10 passed**, including warm failedRequests=1 from a cold aborted fetch, missing app-timing API/header, eager warmup, head script, and avatar attributes. `Task6ServiceWorker.test.js -t 'staff worker serves'` failed after correcting its Request-object fixture because bootstrap/main were absent from install cache. The delayed-install Chromium test failed with zero SW hits before the gate waited for worker activation. The upgrade regression failed because old v414 cache served canonical `main.js` after v415's first versioned script had bypassed it. A broad run found the old structural hash counted the deliberately removed nonvisual `<script>` tag; hashes were recomputed from pinned `e6105ef6` with script elements masked. An intermittent visual pixel test failed once at 10.88% then passed isolated; the renderer now waits for fonts/image decode, and the pinned-baseline visual tests pass in focused and whole-unit runs.

GREEN commands ran from `staff/backend` with `NODE_PATH=D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules` and `PUPPETEER_EXECUTABLE_PATH=C:/Program Files/Google/Chrome/Application/chrome.exe`:

- `node D:/DAF-PROJECT/DOKTERDIBYA/staff/backend/node_modules/jest/bin/jest.js --runInBand --forceExit --coverage=false --silent --testPathPatterns=Task6 tests/unit/StaffStartupPerformance.test.js tests/unit/StaffPanelWave2LazyShell.test.js` — **12 suites / 75 tests passed**.
- The same Jest invocation with `--testPathPatterns=integration` — **23 suites / 240 tests passed**. Existing Staff smoke (Wave1, Wave2, StaffApiClient, observabilityAuth) — **4 suites / 36 tests passed**.
- Whole-unit sweep — **159 suites / 1188 tests passed; 12 suites / 13 tests failed** (171/1201 total). The exact 12 failing suite names and 13 failing test count match the recorded pre-repair baseline; no changed-area suite failed. These pre-existing failures are not claimed fixed.
- `node staff/backend/scripts/staff-static-check.js` passed (HTML 317460 bytes, inline JS 41162 chars, cache v414); `node --check` passed on **11 changed/new JS files**; `git diff --check` passed. The local browser tests use synthetic loopback resources only.

Final upgrade-race RED: extending `Task6ServiceWorker.test.js -t 'old staff controller'` to overlap a v415 document with a late v414 explicit request failed because that old request re-authorized cached canonical imports for the v415 client (**1 test failed**). The worker now treats any explicit version mismatch as sticky for that client while controlled by the old worker; the same targeted test passed (**1 passed**). Map eviction remains safe because unknown clients bypass cache. This keeps all later canonical imports network-backed for the new document even when an old document's request races it.

Self-review: `Server-Timing` measures backend middleware/auth/route preparation until headers, not network RTT or streaming after headers. Chrome CDP flags were checked against the installed v24 protocol types and exercised with real memory-cache, SW-cache, HTTP 503, disconnect, and first-reload fixtures. The optional Staff worker wait is fail-closed on an existing registration that cannot activate; a site without a worker is measured uncached, so it cannot gain a false cache pass. The current-version SW cache cannot answer a future-version document's canonical imports; if client identity is absent, it uses the network. The bounded local fixture demonstrates <=40 genuine network requests, but **CI against the real authenticated shell and live postdeployment RUM/performance remain mandatory release gates**. No live improvement or deployed state is claimed here.

## Wave 3 review fix loop 1 — delayed warm request only

The independent `65d591e0` review found that `networkidle2` can finish with two requests outstanding; the CDP listener detached before a warm-started delayed 503/disconnect completed. Real-Chromium RED command `jest --runInBand --forceExit --coverage=false --silent tests/unit/Task6PerformanceGate.test.js -t "finishing after networkidle2"` failed **2 tests**: both returned `warm.failedRequests=0` despite the delayed server action. The gate now retains CDP observation after warm navigation until all non-Socket.IO-poll warm requests terminate, with a five-second fail-closed deadline. Request-start phase attribution is unchanged; an expected same-origin `/socket.io/?transport=polling` long-poll may remain open. The delayed 503/disconnect tests turned GREEN, and additional Chromium checks prove a stuck ordinary request rejects at the deadline while an open Socket.IO poll does not falsely fail. `Task6PerformanceGate` plus unchanged `Task6ServiceWorker` passed **2 suites / 33 tests**. This fix does not claim any live budget result.

The review's separate Staff SW upgrade finding remains **OPEN**. The controller's read-only Nginx configuration check established that production aliases `/staff/public/` directly from disk and treats any `?v` as immutable; an Express-only stale-version guard would not run. Current v413 Staff worker bypasses script requests, so an already-open v413 document can still fetch current origin bytes under `?v=v413` before a safe versioned-serving transition. A speculative SW/Express guard was tested locally then removed before commit because it could not cover this direct-Nginx window; no forced navigation was adopted because Sunday Clinic has unsaved in-memory drafts protected by `beforeunload`. A Nginx/versioned-asset rollout design requires separate approval and an actual two-version browser staging test. Prior self-review's cross-version statement covered only a future-version document under the old worker cache, not this old-document/new-origin case. Do not release this task as fully approved on the basis of the P1 correction.
