import 'package:flutter/material.dart';

class AppColors {
  // Primary - Bootstrap Blue
  static const Color primary = Color(0xFF0d6efd);
  static const Color primaryLight = Color(0xFF6ea8fe);
  static const Color primaryDark = Color(0xFF0a58ca);

  // Secondary
  static const Color secondary = Color(0xFF6c757d);
  static const Color secondaryLight = Color(0xFFa2a9b1);

  // Success
  static const Color success = Color(0xFF198754);
  static const Color successLight = Color(0xFFd1e7dd);

  // Danger
  static const Color danger = Color(0xFFdc3545);
  static const Color dangerLight = Color(0xFFf8d7da);

  // Warning
  static const Color warning = Color(0xFFffc107);
  static const Color warningLight = Color(0xFFfff3cd);

  // Info
  static const Color info = Color(0xFF0dcaf0);
  static const Color infoLight = Color(0xFFcff4fc);

  // Background
  static const Color background = Color(0xFFf4f6f9);
  static const Color surface = Color(0xFFffffff);
  static const Color cardBg = Color(0xFFffffff);

  // Text
  static const Color textPrimary = Color(0xFF212529);
  static const Color textSecondary = Color(0xFF6c757d);
  static const Color textMuted = Color(0xFF9ca3af);

  // Border
  static const Color border = Color(0xFFdee2e6);
  static const Color divider = Color(0xFFe9ecef);

  // Category Colors
  static const Color obstetri = Color(0xFF17a2b8);
  static const Color gynRepro = Color(0xFF28a745);
  static const Color gynSpecial = Color(0xFFffc107);
}
